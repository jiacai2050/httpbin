{
  "name": "edgebin",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "deploy": "wrangler deploy",
    "gen": "node scripts/gen-index.js",
    "dev": "wrangler dev",
    "start": "wrangler dev",
    "test": "vitest"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.8.19",
    "vitest": "~3.2.0",
    "wrangler": "^4.42.0"
  },
  "dependencies": {
    "marked": "^16.3.0",
    "qrcode": "^1.5.4"
  }
}
